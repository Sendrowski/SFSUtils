# Runtime dependencies for the sfsutils base conda env (pulled in by base.yaml).
# Direct dependencies only; pip resolves the transitive closure. Includes cyvcf2
# for VCF parsing. Keep in sync with [tool.poetry.dependencies] in pyproject.toml.
matplotlib>=3.9,<4
seaborn>=0.13.2,<0.14
numpy>=1.24,<3
pandas>=2.2.2,<3
jsonpickle>=3.0.0,<4
multiprocess>=0.70.12,<0.71
tqdm>=4.60.0,<5
biopython>=1.84,<2
requests>=2.28
scipy>=1.13,<2
cyvcf2>=0.31.0,<0.32
