# Development environment: the simulation stack used to (re)generate the msprime SFS-validation
# fixtures (msprime + tskit), the test toolchain, and the Sphinx/myst-nb docs stack for building
# and re-executing the reference notebooks.
#
# Create the env, then install the package editable with the optional cyvcf2 VCF backend (from
# PyPI, since cyvcf2 is not on conda-forge), run from the repository root:
#   mamba env create -f envs/dev.yaml
#   mamba run -n sfsutils-dev pip install -e ".[vcf]"
#   mamba activate sfsutils-dev
name: sfsutils-dev
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  # --- simulation / ground-truth SFS for the parser-validation fixtures ---
  - msprime
  - tskit
  # --- testing ---
  - pytest
  - pytest-xdist
  - pytest-cov
  - pytest-sugar
  # --- docs (build + re-execute notebooks) ---
  - nbconvert
  - sphinx
  - myst-nb
  - sphinx-book-theme
  - sphinx-autodoc-typehints
  - sphinx-copybutton
  - sphinxcontrib-bibtex
  - autodocsumm
